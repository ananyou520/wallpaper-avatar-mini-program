<view class="bzWrapper">
    <view bindtap="onItem" class="item" id="{{idx}}" wx:for="{{dataList}}" wx:for-index="idx" wx:key="item">
        <image mode="aspectFill" src="{{item.thumb}}"></image>
    </view>
</view>
